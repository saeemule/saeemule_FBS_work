#Take input of base and height
base = float(input('Enter base of triangle : '))
height = float(input('Enter height of triangle : '))
 
#Perform calculation
area = 0.5 * base * height
 
#Perform result
print(f'Area of triangle with {base} and {height} is {area}.')