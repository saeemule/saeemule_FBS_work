#Take input of two angles
angle1 = float(input('Enter first angle: '))
angle2 = float(input('Enter second angle: '))

#Perform calculation
angle3 = 180 - (angle1 + angle2)

#Perform result
print(f'The third angle of the triangle is {angle3} degrees.')