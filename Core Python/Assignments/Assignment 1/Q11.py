#Take input of radius
radius = float(input('Enter radius of circle: '))
 
#Perform calculation
circumference = 2 * 3.14 * radius

#Perform result 
print(f'Circumference of circle with {radius} is {circumference}.')